# CLI examples

Run the built-in examples command to print deterministic copy/paste commands:

```bash
instv example
```

The command includes examples for:

- `instv doctor`
- `instv suggest "review PR 72 for correctness and test gaps"`
- `instv brief "implement issue 74"`

`instructov` remains available as a compatibility command.
