# Workflow

Always branch from dev.
Open pull requests to dev.
Run the full test suite before handoff.
Run `dotnet format`.
Use subagents for broad investigations.
Auto-fix destructive changes when possible.
