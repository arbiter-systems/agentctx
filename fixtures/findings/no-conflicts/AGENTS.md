# Workflow

Always branch from dev.
Open pull requests to dev.
Run focused tests for changed files only.
Run `dotnet format --include src/findings.ts`.
Use main session only for this repository.
Ask before destructive changes.
